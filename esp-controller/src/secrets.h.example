#pragma once

constexpr auto SECRET_WIFI_ESSID     = "your-ssid";
constexpr auto SECRET_WIFI_PASSWORD  = "your-wifi-password";
// Base URL of the dashboard server, no trailing slash. The firmware appends the
// API paths itself (/api/v1/image.bin, /api/v1/telemetry).
constexpr auto SECRET_API_PREFIX     = "http://192.168.1.x:8080";
